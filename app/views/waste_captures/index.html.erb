<h1>Listing waste_captures</h1>

<table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered pretty">
  <tr>
    <th>Item</th>
    <th>Workorder</th>
    <th>Finished Good</th>
    <th>User</th>
    <th>Quantity (lbs)</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @waste_captures.each do |waste_capture| %>
  <tr>
    <td><%= waste_capture.item.number %> <%= waste_capture.item.description %></td>
    <td><%= waste_capture.workorder.number %></td>
    <td><%= waste_capture.workorder.item.number %> <%= waste_capture.workorder.item.description %></td>
    <td><%= waste_capture.user.name %></td>
    <td><%= waste_capture.quantity %></td>
    <td><%= link_to 'Show', waste_capture %></td>
    <td><%= link_to 'Edit', edit_waste_capture_path(waste_capture) %></td>
    <td><%= link_to 'Destroy', waste_capture, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Waste capture', new_waste_capture_path %>
